# Force CRLF line endings for Windows batch files so cmd.exe can parse them
# regardless of the user's core.autocrlf setting.
*.bat text eol=crlf

# Production bundles may contain template literals whose final space/tab is
# data for bundled third-party parsers. Do not flag those bytes as source
# formatting errors during the required diff check.
static/assets/*.js -whitespace
